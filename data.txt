//mobile friendly
const mobileToggle = document.getElementById("mobileToggle");
const mobileMenu = document.getElementById("mobileMenu")
const postElementBtn = document.getElementsByClassName('quick-post-btn')
const Avatar = document.getElementsByClassName('user-avatar')
const dropDownElements = document.getElementsByClassName('dropdown-menu')
// Toggle Mobile Menu
mobileToggle.addEventListener("click", () => {
    if(mobileToggle.className == 'mobile-toggle'){
        mobileToggle.className = 'mobile-toggle-active'
        


    }
    else{
        mobileToggle.className = 'mobile-toggle'

    }
});

given that with
mobileToggle -(button) a button that toggles(opens  and closes the side panel in mobile)
mobileMenu -(div) the side Panel that covers the left vertical side of the phone and 1/3 of the horizontal side when opened and disappears to the left in a sliding animation
postElementBtn -(button) a button in the web in desktop that when a mouse hovers over it it shows a dropdown list of options of posting(question, answer) and disappears in mobile because the dropdown goes to the mobileMenu
Avatar -(div) circular profile icon where the user sees their profile picture or first letters of the first and lastname(eg JK if the user does not have a profile picture)